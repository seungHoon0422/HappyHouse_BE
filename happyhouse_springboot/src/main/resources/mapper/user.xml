<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.house.model.mapper.UserMapper">

	<!-- 회원가입 -->
	<insert id="registerMember" parameterType="UserDto">
		insert into user(userid, userpass, email, username, phone, level)
		values (#{userid},#{userpass},#{email},#{username},#{phone},#{level})
	</insert>

	<!-- 로그인 -->
	<select id="login" parameterType="map" resultType="UserDto">
		select username, userid, level
		from user
		where userid=#{userid} and userpass = #{userpass} 
	</select>
	
	<!-- 회원정보조회 -->
	<select id="info" parameterType="UserDto" resultType="UserDto">
		select username, userid, userpass, email, phone 
		from user
		where username=#{username} and userid=#{userid}
	</select>
	
	<!-- 회원 정보 삭제 (탈퇴) -->
	<delete id="delete" parameterType="UserDto">
		delete from user
		where userid=#{userid}
	</delete>
	
	<!-- 회원 정보 수정 -->
	<update id="update" parameterType="UserDto">
		update user
		set userpass=#{userpass}
		where userid= #{userid} 
	</update>
	
	<!-- 관심목록 조회 -->
	<selectAll id="interest" parameterType="UserDto" resultType="InterestDto">
	
	</select>

</mapper>